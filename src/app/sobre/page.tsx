import { Metadata } from "next";

const metadata: Metadata = {
    title: 'Sobre'
}

const Page = () => {
    return(
        <div>
            Página Sobre
        </div>
    )
}

export default Page;